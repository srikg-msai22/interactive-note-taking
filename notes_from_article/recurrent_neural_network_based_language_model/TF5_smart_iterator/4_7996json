"<pad> in statistical lan- guage modeling usually differs in the fact that models do not get updated as test data are being processed. for dynamic model, we use fixed learning rate where f (z) is sigmoid activation function: once as it processes testing data. if 'dog' and 'cat' are related, frequent occurrence of 'dog' in test- ing data will also trigger increased probability of 'cat' network learns in continuous space, so if 'dog'- a- a</s>"