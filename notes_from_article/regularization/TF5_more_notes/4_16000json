"<pad> we propose a non-monotoniccriterion that conservatively triggers the averaging when the validation metric fails to improve for multiple cycles. we use this setting in all of our NT-ASGD experiments anddemon-strate that it achieves better training outcomes as compared tosGD. we also explored additional regularization tech-niques that aimed to improve data efficiency during training. we found that a fixed sequence length that is used to break a dataset into fixed length batches is a<extra_id_27> a </s>"