"<pad> weight tying (inan et al., 2016) shares weights between the embedding and softmax layer, sub-stantially reducing the total parameter count in the model.4.4. activation regularization (AR) and temporal activation regularization (TAR) can be used to control the norm of the resulting model and reduceoverfitting.4.7. weight tying (inan et al., 2016) shares the weights between the embedding and softmax layer, sub- - a </s>"