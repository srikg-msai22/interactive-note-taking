"<pad> each of the layers in our encoder and decoder contains a fully connected feed-forward network. this consists of two linear transformations with a ReLU activation in between. the dimensionality of input and output is d_(model) = 512, and the inner-layer has dimensionality d_(ff) = 2048. each of the layers in our encoder and decoder contains a fully connected feed-forward network, which is applied to each position separately and identically</s>"