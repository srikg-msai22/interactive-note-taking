"<pad> we compare different model architectures for deriving the word vectors using the same training data and using the same dimensionality of the word vectors. we use full set of questions in the new Semantic-Syntactic Word Relationship test set, i.e. unrestricted to the 30k vocabulary. the word vectors from the RNN (as used in [20]) perform well mostly on the syntactic questions. the NNLM vectors perform significantly better a a a a.......... a a a a a. a a a a...... a. ena a a a a..... </s>"