"<pad> if a new person- name occurs repeatedly in the test set, it will repeatedly get a very small probability, even if it is composed of known words. for dynamic model, we use fixed learning rate where f (z) is sigmoid activation function: once as it processes testing data. network is trained in several epochs, in which all data from training corpus sequentially presented. weights are initialized to small values (random Gaussian noise with zero mean and 0.1 -. a</s>"