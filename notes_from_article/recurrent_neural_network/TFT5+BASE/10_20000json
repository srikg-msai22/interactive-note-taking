"<pad> if we want to build models that can really learn language, then on-line learning is crucial. it does not seem that simple recurrent neural networks can capture truly long context infor- mation. as we did not make any task or language specific assump- tion in our work, it is easy to use RNN based models almost ef- fortlessly in any kind of application that uses backoff language models, like machine translation or OCR. it is possible that further investigation into backpro-- - a</s>"